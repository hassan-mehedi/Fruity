<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fruitinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fruit_64"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fruit_64"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SeasonDetails" />
        <activity android:name=".FruitDetails" />
        <activity android:name=".FruitRelatedVideos" />
        <activity android:name=".NationalFruits" />
        <activity android:name=".FruitsVitamin" />
        <activity android:name=".SeasonalFruits" />
        <activity
            android:name=".FruitySplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>